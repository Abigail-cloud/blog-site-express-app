require('dotenv').config();

// Your web app's Firebase configuration
let firebaseConfig = {
 //Appconfiguration on firebase
};

firebase.initializeApp(firebaseConfig);

let db = firebase.firestore();





